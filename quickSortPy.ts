const sorter = (
  array: number[],
  lowestIndex: number,
  highestIndex: number
) => {};

const quickSort = (array: number[]) => {
  const sortedArray = sorter(array, 0, array.length - 1);

  return sortedArray;
};
